{
  "name": "vilicando",
  "private": true,
  "version": "0.0.1",
  "license": "ISC",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "dev": "cd examples/basic && yarn link vilicando-core && yarn dev",
    "up": "yarn upgrade-interactive --latest && yarn check"
  },
  "workspaces": [
    "core",
    "examples/basic"
  ]
}
